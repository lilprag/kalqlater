import Link from 'next/link';
import { notFound, permanentRedirect } from 'next/navigation';
import { JsonLd } from '../../../../components/JsonLd';
import { breadcrumbJsonLd, pageMetadata } from '../../../../lib/metadata';
import { isLocale, localePath, productionAppUrl } from '../../../../lib/site';
import { allPairs, comparisonProfile, parsePair } from '../../../../lib/comparisons';
import { personalityProfile } from '../../../../lib/personality';

export function generateStaticParams() { return ['en', 'hi'].flatMap((locale) => allPairs().map((pair) => ({ locale, pair: pair.slug }))); }
export const dynamicParams = false;

export async function generateMetadata({ params }) { const { locale, pair } = await params; const parsed = parsePair(pair); if (!isLocale(locale) || !parsed) return {}; const first = personalityProfile(parsed.canonical[0], locale); const second = personalityProfile(parsed.canonical[1], locale); return pageMetadata({ locale, path: `compare/${parsed.slug}`, title: `${first.code} vs ${second.code}: ${locale === 'hi' ? 'रिलेशनशिप इंटेलिजेंस' : 'Relationship Intelligence'}`, description: locale === 'hi' ? `${first.displayName} और ${second.displayName} संवाद, निर्णय और सहयोग में कैसे अलग या पूरक हो सकते हैं।` : `Explore how ${first.displayName} and ${second.displayName} may differ or complement one another in communication, decisions, and collaboration.` }); }

export default async function ComparisonPage({ params }) { const { locale, pair } = await params; if (!isLocale(locale)) notFound(); const parsed = parsePair(pair); if (!parsed) notFound(); if (!parsed.isCanonical) permanentRedirect(localePath(locale, `compare/${parsed.slug}`)); const [firstCode, secondCode] = parsed.canonical; const first = personalityProfile(firstCode, locale); const second = personalityProfile(secondCode, locale); const insight = comparisonProfile(firstCode, secondCode, locale); const hi = locale === 'hi'; const labels = hi ? { strength:'साथ में ताकत', differences:'मुख्य अंतर', communication:'संवाद', decisions:'निर्णय लेना', conflict:'मतभेद की संभावना', work:'काम और सहयोग', friendship:'दोस्ती', romance:'रिश्ते के विचार', growth:'विकास के अवसर', tips:'व्यावहारिक सुझाव', related:'संबंधित तुलनाएँ', test:'व्यक्तित्व टेस्ट दें', note:'ये अंतर्दृष्टियाँ आत्मचिंतन के लिए हैं, निश्चित संगतता का दावा नहीं।' } : { strength:'Strength together', differences:'Key differences', communication:'Communication', decisions:'Decision-making', conflict:'Possible friction', work:'Work and collaboration', friendship:'Friendship', romance:'Romantic relationship considerations', growth:'Growth opportunities', tips:'Practical tips', related:'Related comparisons', test:'Take the personality test', note:'These insights support reflection; they do not claim certain compatibility.' }; const sections = [[labels.strength, insight.attraction], [labels.differences, `${first.code}: ${first.shortSummary} ${second.code}: ${second.shortSummary}`], [labels.communication, insight.communication], [labels.decisions, `${insight.decisionA} ${insight.decisionB} ${insight.decisions}`], [labels.conflict, insight.conflict], [labels.work, insight.advice], [labels.friendship, insight.friendship], [labels.romance, insight.romantic.longTerm], [labels.growth, insight.growth], [labels.tips, insight.advice]]; const related = allPairs().filter((item) => item.slug !== parsed.slug && (item.first === firstCode || item.second === firstCode || item.first === secondCode || item.second === secondCode)).slice(0, 3); const jsonLd = breadcrumbJsonLd(locale, [{ name:'KalQLater' }, { name: hi ? 'तुलना' : 'Compare', path:'compare' }, { name:`${firstCode} vs ${secondCode}`, path:`compare/${parsed.slug}` }]); return <><JsonLd data={jsonLd} /><article className="mx-auto max-w-5xl px-4 py-12 sm:px-6 lg:px-8"><header className="rounded-[2rem] bg-brand-ink p-7 text-white sm:p-10"><p className="text-xs font-semibold uppercase tracking-[.22em] text-brand-sand">{hi ? 'रिलेशनशिप इंटेलिजेंस' : 'Relationship Intelligence'}</p><h1 className="display-font mt-3 text-4xl sm:text-6xl">{first.code} <span className="text-brand-sand">vs</span> {second.code}</h1><p className="mt-5 max-w-3xl text-lg leading-relaxed text-white/75">{insight.label} · {insight.attraction}</p><p className="mt-6 rounded-2xl bg-white/10 p-4 text-sm text-white/80">{labels.note}</p></header><div className="mt-8 grid gap-5 md:grid-cols-2">{sections.map(([title, body]) => <section key={title} className="rounded-[1.75rem] border border-brand-line bg-white p-6"><h2 className="display-font text-2xl">{title}</h2><p className="mt-4 leading-relaxed text-brand-subtle">{body}</p></section>)}</div><section className="mt-8"><h2 className="display-font text-3xl">{labels.related}</h2><div className="mt-4 flex flex-wrap gap-3">{related.map((item) => <Link key={item.slug} href={localePath(locale, `compare/${item.slug}`)} className="rounded-full border border-brand-line bg-white px-4 py-2 font-semibold hover:border-brand-teal">{item.first} vs {item.second}</Link>)}</div></section><div className="mt-10 flex flex-wrap gap-3"><Link href={localePath(locale, `personality/${first.slug}`)} className="rounded-full border border-brand-line bg-white px-6 py-3 font-semibold">{first.code}</Link><Link href={localePath(locale, `personality/${second.slug}`)} className="rounded-full border border-brand-line bg-white px-6 py-3 font-semibold">{second.code}</Link><a href={productionAppUrl('/test')} className="rounded-full bg-brand-teal px-6 py-3 font-semibold text-white">{labels.test}</a></div></article></>; }
