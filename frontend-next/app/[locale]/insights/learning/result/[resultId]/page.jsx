import{LearningResult}from'../../../../../../components/insights/LearningResult';import{pageMetadata}from'../../../../../../lib/metadata';export async function generateMetadata({params}){const{locale}=await params;return pageMetadata({locale,path:'insights/learning/result',title:'Learning Insights',description:'Private result.',noIndex:true})}export default async function P({params}){const{locale,resultId}=await params;return <LearningResult locale={locale} resultId={resultId}/>}
