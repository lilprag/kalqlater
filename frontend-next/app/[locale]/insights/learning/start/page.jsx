import{LearningStart}from'../../../../../components/insights/LearningAssessment';import{pageMetadata}from'../../../../../lib/metadata';export async function generateMetadata({params}){const{locale}=await params;return pageMetadata({locale,path:'insights/learning/start',title:'Learning Insights',description:'Private reflection.',noIndex:true})}export default async function P({params}){const{locale}=await params;return <LearningStart locale={locale}/>}
