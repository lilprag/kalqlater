import{LearningFlow}from'../../../../../../components/insights/LearningAssessment';import{pageMetadata}from'../../../../../../lib/metadata';export async function generateMetadata({params}){const{locale}=await params;return pageMetadata({locale,path:'insights/learning/session',title:'Learning Insights',description:'Private assessment.',noIndex:true})}export default async function P({params}){const{locale,sessionId}=await params;return <LearningFlow locale={locale} sessionId={sessionId}/>}
