"use client";import{useEffect,useState}from'react';import Link from'next/link';import{jobsApiUrl}from'../lib/jobs-api';export function SavedJobsClient({locale='en'}){const[rows,setRows]=useState([]);useEffect(()=>{fetch(jobsApiUrl('/saved'),{headers:{Authorization:`Bearer ${localStorage.getItem('kalqlater_auth_token')||''}`}}).then(r=>r.ok?r.json():[]).then(setRows)},[]);return <main className="mx-auto max-w-5xl px-4 py-12"><h1 className="display-font text-5xl">Saved jobs</h1><div className="mt-8 space-y-4">{rows.map(j=><article className="rounded-3xl border bg-white p-6" key={j.id}><p>{j.company_name}</p><h2 className="text-2xl font-bold"><Link href={`/${locale}/jobs/${j.slug}`}>{j.title}</Link></h2><p>{j.location_text} · {j.work_mode}</p></article>)}{!rows.length&&<p>No saved jobs yet.</p>}</div></main>}
