import React from 'react'; import { motion } from 'framer-motion';
export default function ComparisonSection({ title, children, index=0, tone='light' }) { return <motion.section initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.15}} transition={{duration:.42,delay:Math.min(index*.04,.2)}} className={`rounded-[1.75rem] border p-6 shadow-[0_12px_36px_rgba(45,40,37,.04)] sm:p-8 ${tone==='dark'?'border-white/10 bg-[#24343A] text-white':'border-brand-line bg-white text-brand-ink'}`}><p className={`text-xs font-semibold uppercase tracking-[.22em] ${tone==='dark'?'text-brand-sand':'text-brand-teal'}`}>Relationship intelligence</p><h3 className="mt-2 text-2xl font-display">{title}</h3><div className={`mt-5 leading-relaxed ${tone==='dark'?'text-white/75':'text-brand-subtle'}`}>{children}</div></motion.section>; }
